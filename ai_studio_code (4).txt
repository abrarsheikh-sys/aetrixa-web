"use client";

import React from "react";
import { motion } from "framer-motion";

const partners = [
  "CONDÉ NAST",
  "TRAVEL + LEISURE",
  "FOUR SEASONS",
  "AMAN",
  "ROSEWOOD"
];

export default function TrustBadges() {
  return (
    <div className="w-full bg-aetrixa-charcoal border-b border-white/5 py-12 relative z-10">
      <div className="max-w-7xl mx-auto px-6">
        <p className="text-center text-aetrixa-ivory/30 text-[10px] uppercase tracking-[0.3em] font-sans mb-8">
          Trusted by Elite Partners
        </p>
        <div className="flex flex-wrap justify-center items-center gap-12 md:gap-24 opacity-60 grayscale hover:grayscale-0 transition-all duration-500">
          {partners.map((partner, index) => (
            <motion.div
              key={partner}
              initial={{ opacity: 0 }}
              whileInView={{ opacity: 1 }}
              viewport={{ once: true }}
              transition={{ delay: index * 0.1, duration: 0.5 }}
              className="font-serif text-xl md:text-2xl text-aetrixa-gold"
            >
              {/* Replacing logos with Serif text for the placeholder */}
              {partner}
            </motion.div>
          ))}
        </div>
      </div>
    </div>
  );
}